{
  "name": "shared",
  "version": "1.0.0",
  "license": "ISC",
  "scripts": {
    "test": "jest",
    "lint": "eslint",
    "format": "prettier",
    "build": "tsc"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
