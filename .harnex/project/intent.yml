# The domain map for micdrp. A domain is a vertical slice: it owns its
# entities, capabilities, invariants, and every interface it exposes.
#
# All six domains are now specified and registered in _registry.yml.
# Anything new goes here first, then moves to the registry once its specs
# validate, so the registry never holds a half-specified domain.

future_domains: []
