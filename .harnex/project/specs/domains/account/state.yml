# Domain Development State — overwrite each meaningful change
updated_at: "2026-09-16T00:00:00Z"
updated_by: agent
phase: building
phase_detail: >
  Rotating refresh tokens built (INV-ACCOUNT-016..023). Backend: pb_hooks/
  session.pb.js + lib/session.js, migration 1787900000 (refresh_tokens,
  access token 1h). Built-in auth-refresh refused. Client: auth/refreshToken
  (Keychain), renewSession (single-flight, 401 only clears), useSessionRenewal
  (launch, foreground, beforeSend), sessionState.hasSession used by
  AuthContext and requireUserId. Verified: backend/verify-session.mjs 15/15
  on local PocketBase; 11 client tests in auth/__tests__/renewSession.test.tsx.
next: >
  Sign in with Apple (chosen: Apple only). Native sheet, identity token
  verified by a hook against Apple JWKS, answered with the same token pair.
  Needs the Sign in with Apple capability on the app id and a native build.
blockers: []
known_conflicts:
  - >
    test-extensions.yml is over the 150-line spec-file budget (harnex#153).
    AuthContext.tsx 181 lines. renew/adopt capabilities have no acceptance
    tests: the account adapter does not exist, so they would only SKIP.
